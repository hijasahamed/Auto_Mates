import 'package:flutter/material.dart';

class SpecificationFeaturesWidget extends StatelessWidget {
  const SpecificationFeaturesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // return DefaultTabController(
    //   length: length, 
    //   child: child
    // )
    return Text('sdadf');
  }
}