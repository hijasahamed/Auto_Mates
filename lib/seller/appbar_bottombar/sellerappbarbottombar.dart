import 'package:flutter/material.dart';

class Sellerappbarbottombar extends StatelessWidget {
  const Sellerappbarbottombar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('Seller AppBottomBAr'),
    );
  }
}