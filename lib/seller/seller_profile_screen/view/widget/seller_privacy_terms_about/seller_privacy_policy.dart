import 'package:flutter/material.dart';

class SellerPrivacyPolicy extends StatelessWidget {
  const SellerPrivacyPolicy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Privacy Policy'),
    );
  }
}