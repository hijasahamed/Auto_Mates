import 'package:flutter/material.dart';

class InsurenceDropdown extends StatelessWidget {
  const InsurenceDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}