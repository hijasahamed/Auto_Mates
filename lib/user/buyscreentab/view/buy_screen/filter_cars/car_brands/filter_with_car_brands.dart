import 'package:flutter/material.dart';

class FilterWithCarBrands extends StatelessWidget {
  const FilterWithCarBrands({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
