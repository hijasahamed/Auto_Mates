import 'package:flutter/material.dart';

class RelatedCars extends StatelessWidget {
  const RelatedCars({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        height: 300,
        width: double.maxFinite,
        color: Colors.white,
      ),
    );
  }
}